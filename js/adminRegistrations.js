(async()=>{const c=window.supabase?.createClient(window.SUPABASE_CONFIG?.url,window.SUPABASE_CONFIG?.key);const {data}=await c?.auth.getUser()||{};if(String(data?.user?.email||'').toLowerCase()!=='sixxset@ya.ru'){location.href='/app/districts';return}const {data:s}=await c.auth.getSession();const r=await fetch(`${window.SUPABASE_CONFIG.url}/functions/v1/workspace-api/admin/registrations`,{headers:{Authorization:`Bearer ${s.session.access_token}`}});const box=document.getElementById('registration-events');if(!r.ok){box.textContent='Доступ запрещён';return}const rows=await r.json();box.innerHTML=rows.length?rows.map(x=>`<div class="history-item"><strong>${x.event_type}</strong><span>${x.email||''} · ${new Date(x.created_at).toLocaleString('ru-RU')}</span></div>`).join(''):'<p>Событий пока нет.</p>'})();
